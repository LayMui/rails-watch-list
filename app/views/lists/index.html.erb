<div class="container">
  <h1>All lists</h1>
  <div class="row">
    <% @lists.each do |list| %>
      <div class="col-md-4">
        <%= link_to list.title, list_path(list) %>
      </div>
    <% end %>
  </div>
</div>
